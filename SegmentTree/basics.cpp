#include <bits/stdc++.h>
using namespace std;

class SegmentTree {
private:
    vector<int> tree;

    void buildTree(int i, int l, int r, vector<int> &arr) {
        
    }

public:
    SegmentTree(int n) {
        tree.assign(4*n, 0);
    }
};

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);



    return 0;
}