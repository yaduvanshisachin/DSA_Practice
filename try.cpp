#include <iostream>
#include <unordered_map>
#include <queue>
#include <stack>
#include <list>
using namespace std;

int main() {

    // vector<int> arr(); // arr indexing is not allowed
    vector<int> arr(5); // [0, 0, 0, 0, 0]

    // int arr[2] = {5, 10} ;

    cout << arr[0];

}